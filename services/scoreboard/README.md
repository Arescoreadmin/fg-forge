# forge_scoreboard

Scoring service responsible for deterministic grading and signing. Expected
responsibilities:

- Aggregate successCriteria evidence from worker agents.
- Produce `score.json`, `evidence.tar.zst`, and `verdict.sig`.
- Store signed artifacts in MinIO.

Status: stub.
