# forge_egress_gateway

Egress gateway enforcing deny-all by default with optional allowlist profiles.
Expected responsibilities:

- Apply nftables profiles per scenario network.
- Emit egress deny/allow logs for audit.

Status: stub.
