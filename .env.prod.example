FORGE_ENV=prod
LOG_LEVEL=INFO

# This should be the public URL behind your ingress/LB
SPAWN_BASE_URL=https://frostgate.ai

OPA_URL=http://forge_opa:8181
NATS_URL=nats://forge_nats:4222
REDIS_URL=redis://forge_redis:6379
SCOREBOARD_URL=http://forge_scoreboard:8080
MINIO_ENDPOINT=forge_minio:9000
SIGNING_KEY_PATH: /app/secrets/scoreboard_ed25519.pem


MINIO_ROOT_USER=d36c7613455f7c7f
MINIO_ROOT_PASSWORD=REPLACE_ME
MINIO_BUCKET=forge-evidence
SANITIZED_BUCKET=forge-sanitized

BILLING_MODE=stripe

# REQUIRED secrets (your code enforces these outside dev)
ACCESS_TOKEN_SECRET=REPLACE_ME
SAT_HMAC_SECRET=REPLACE_ME
ET_HMAC_SECRET=REPLACE_ME
RECEIPT_HMAC_SECRET=REPLACE_ME
OPERATOR_TOKEN=REPLACE_ME
ORCHESTRATOR_INTERNAL_TOKEN=REPLACE_ME
SCOREBOARD_INTERNAL_TOKEN=REPLACE_ME
SCOREBOARD_SIGNING_KEY=REPLACE_ME









# --- Ingress (Caddy) ---
FORGE_DOMAIN=frostgate.ai
FORGE_ACME_EMAIL=contact@frostgate.ai

# Public base URL (same domain)
EGRESS_GATEWAY_PUBLIC_URL=https://frostgate.ai

# CAP token signing + runtime knobs
CAP_TOKEN_SECRET=REPLACE_ME
CAP_TOKEN_TTL_SECONDS=300
TTY_IDLE_SECONDS=900
TTY_MAX_SECONDS=1800
DOCKER_GID=0
FORGE_DOMAIN=frostgate.ai
FORGE_ACME_EMAIL=contact@frostgate.ai
